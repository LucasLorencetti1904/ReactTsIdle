export default class {
    public name: string;
    public tier: number;

    public constructor(
        name: string,
        tier: number
    ) {
        this.name = name;
        this.tier = tier;
    }
}
